import * as React from 'react'
import * as ReactDOM from 'react-dom'
import { App } from './App'
ReactDOM.render(
  <React.StrictMode>
    <App name="React Shell App" />
  </React.StrictMode>,
  document.getElementById('app')
)
